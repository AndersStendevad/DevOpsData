
name: Build LaTeX report

on:
  push:
    branches: [ report, main ]

jobs:
  -name: Build LaTeX report
  runs-on: ubuntu-latest
  uses: xu-cheng/latex-action@v2
  working_directory: ./report/
  with:
    root_file: main.tex